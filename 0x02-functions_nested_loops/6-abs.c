#include "main.h"

/**
 * print_alphabet - Entry point
 * Description: return number without sign
 * Return: int
 */


int _abs(int n)
{

	if (n > 0)
		return (n);

	else if (n < 0)
		return (-n);

	else
		return (n);
}
